"use strict"

import {allCheck} from "./conts.js";
import {searchElement} from "./functions";

export function showPrice() {
    const ofSaleSum = 0;
    const withSaleSum = 0;

    for (let sum of allCheck) {
        const mobile = searchElement(sum, '.product')

    }
}
